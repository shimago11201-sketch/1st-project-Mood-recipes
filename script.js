// Данные рецептов
const recipes = {
  "грустно-быстро": {
    title: "Горячий шоколад за 5 минут",
    text: "1. Нагрей 200 мл молока в микроволновке (1–1.5 мин).\n2. Добавь 2 ч.л. какао + 1–2 ч.л. сахара.\n3. Перемешай. Можно добавить зефирки."
  },
  "грустно-нормально": {
    title: "Бананово-шоколадный тост",
    text: "1. Поджарь хлеб.\n2. Намажь арахисовую пасту.\n3. Положи банан.\n4. Полей шоколадом."
  },
  "энергично-быстро": {
    title: "Энергетический смузи",
    text: "Банан + ягоды + молоко + овсянка + мёд → в блендер на 30 сек."
  },
  "энергично-нормально": {
    title: "Яичница с помидорами",
    text: "Разбей 2–3 яйца, добавь помидор и сыр, жарь 5–6 мин."
  },
  // ... добавь остальные 4 рецепта сам, по аналогии
};

// Находим элементы
const form = document.getElementById("moodForm");
const resultDiv = document.getElementById("result");
const titleEl = document.querySelector(".recipe-title");
const textEl = document.querySelector(".recipe-text");

// Слушаем отправку формы
form.addEventListener("submit", function(e) {
  e.preventDefault(); // предотвращаем перезагрузку страницы

  const mood = document.getElementById("mood").value;
  const time = document.getElementById("time").value;

  if (!mood || !time) {
    alert("Выбери и настроение, и время!");
    return;
  }

  const key = mood + "-" + time;
  const recipe = recipes[key];

  if (recipe) {
    titleEl.textContent = recipe.title;
    textEl.textContent = recipe.text;
    resultDiv.classList.remove("hidden");
  } else {
    titleEl.textContent = "Ой...";
    textEl.textContent = "Для такого сочетания пока нет рецепта 😅";
    resultDiv.classList.remove("hidden");
  }
});